@page
@model WebApp.Pages_IngredientLocations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IngredientLocation[0].IngredientUnitName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.IngredientLocation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IngredientUnitName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IngredientLocationId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IngredientLocationId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IngredientLocationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
