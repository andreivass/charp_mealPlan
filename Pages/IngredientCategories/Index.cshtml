@page
@model WebApp.Pages_IngredientCategories.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IngredientCategory[0].IngredientCategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.IngredientCategory) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IngredientCategoryName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IngredientCategoryId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IngredientCategoryId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IngredientCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
